/**
 * A simple test case.
 */
@namespace("imports")
protocol Protocol {
	import idl "AnnotatorTestDataImported.avdl";

	// TODO: When IDL imports are supported, move this declaration to AnnotatorTestDataImported.avdl
	record Employee {
		string name;
		ContractType contract_type;
	}

	record NotAnError {
		string description = "";
	}

	Employee addEmployee(string name, ContractType contract_type) throws Failure, NotAnError;
}
